Un archivo comprimido de menos de 5MB


3 archivos HTML
un barra de navegación entre las páginas   xxx
una lista                                                     xxx
una tabla|
un formulario
una imágen			  xxx
un captacha manual con js, debe versecorrecto o no sobre el html
usar flexbox además del nav
misma apariencia entre las páginas
Utilizar html semántica
ser responsive e movile first (el nav debe estar oculto y desplegarse)
los datos de la tabla deben estar en un JSON y se deben actualizar automáticamente por ingreso de un nuevo dato y cuando se hace una carga de la página. Además un botón de vaciar la tabla

un botón que permita cargar 3 items automaticamente ->pordría ser un generador de 3 usuarios random en la tabla.


un formulario para que los usuarios dejen sus opiniones, lo cargan mediante un form y se muestra en una tabla que muestra nombre y busca por una api rest el comentario del usuario.

Además tiene un boton para generar 3 comentarios random con Loren que luego de 15 segundos se borran para que el usuario vea como quedan.

la tabla solo muestra los últimos 3 comentario, al apretar más comentarios se muestra los últimos 3 comentarios siguientes, al ir agregando comentarios volvemos a mostrar los últimos 3 incluyendo el que se agrega.